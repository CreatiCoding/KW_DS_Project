#include "Manager.h"

/*  ?? ??�� ��?? ?? ?? ?ּ? ??��
    ???? ???Ǽ?�� ��?? ?Ϻ? ?Լ??? ?ڵ尡 ?ۼ??Ǿ? ??��??, ?ش? ?ڵ??? ?̿? ?Ǵ? ??��, ?????? ??????.
    ????, ?Ϻ? ?ڵ??? ??�� ?䱸???׿? ?°? ?????? ?ʿ??? ?? ??�� (??: ?ܼ? ???? -> ???? ????)
    ?? ??, ��???? ?��?��Ʈ ?????? Ŭ????, ???? ????, ???? ?Լ??? ?̸??? ????�� ???? ?Ұ???
    ?? ��???? ?Լ????? ?ڵ带 ?״??? ?̿??? ????, ?ٽ? ?ڵ忡?? ?ݵ??? ?ּ? ?߰?
    */
int main(void){
	cout.setf(ios::fixed);
	cout.precision(2);

	int bpOrder = 3;

	Manager ds(bpOrder);
	ds.run("command.txt");

	return 0;
}
void test();
/*
int main(void){
	test();
	return 0;
}*/
bool compare( pair< pair< double, int>, StockData* > a, pair< pair< double, int>, StockData* > b ){
	//수익률의 내림차순 수익률이 같을
	//경우 고유번호의 오름차순	
	if( a.first.first == b.first.first){
		return a.first.second > b.first.second;
	}else{
		return a.first.first < b.first.first;
	}
}

StockData* createNode(int id, const char* name, int o, int c, int v){
	StockData* newNode = new StockData();
	newNode->setStockID(id);
	newNode->setName((char*)name);
	newNode->setOpening_Price(o);
	newNode->setClosing_Price(c);
	newNode->setVolume(v);
	newNode->setEarning(o,c);
	
}
void testAVLInsert(){
	StockData* n1 = createNode(123, "node1", 123, 456, 111);
	StockData* n2 = createNode(124, "node2", 124, 457, 888);
	StockData* n3 = createNode(125, "node3", 125, 800, 988);
	
	ofstream fout;
	AVLTree* t = new AVLTree(&fout);
	t->Insert(n1);
	t->Insert(n2);
	t->Insert(n3);

	delete n1;
	delete n2;
	delete n3;
	delete t;
}
void test(){
	cout<<"test AVLTree Insert start"<<endl;
	testAVLInsert();
	cout<<"test AVLTree Insert finish"<<endl;
}
