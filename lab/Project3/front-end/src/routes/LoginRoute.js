import React from "react";

//console.log(match.params.toon_info_idx);
const LoginRoute = ({ match }) => {
  let contents = {
    fontSize: "large"
  };
  return <div style={contents}>Welcome to about LoginRoute!!</div>;
};

export default LoginRoute;
