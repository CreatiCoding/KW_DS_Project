import React from "react";

//console.log(match.params.toon_info_idx);
const QuestionsRoute = ({ match }) => {
  let contents = {
    fontSize: "large"
  };
  return <div style={contents}>Welcome to about QuestionsRoute!!</div>;
};

export default QuestionsRoute;
