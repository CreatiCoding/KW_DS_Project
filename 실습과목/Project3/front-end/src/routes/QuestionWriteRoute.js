import React from "react";

//console.log(match.params.toon_info_idx);
const QuestionWriteRoute = ({ match }) => {
  let contents = {
    fontSize: "large"
  };
  return <div style={contents}>Welcome to about QuestionWriteRoute!!</div>;
};

export default QuestionWriteRoute;
