import React from "react";

//console.log(match.params.toon_info_idx);
const JoinRoute = ({ match }) => {
  let contents = {
    fontSize: "large"
  };
  return <div style={contents}>Welcome to about JoinRoute!!</div>;
};

export default JoinRoute;
