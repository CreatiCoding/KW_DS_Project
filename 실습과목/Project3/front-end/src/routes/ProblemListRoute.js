import React from "react";

//console.log(match.params.toon_info_idx);
const ProblemListRoute = ({ match }) => {
  let contents = {
    fontSize: "large"
  };
  return <div style={contents}>Welcome to about ProblemListRoute!!</div>;
};

export default ProblemListRoute;
